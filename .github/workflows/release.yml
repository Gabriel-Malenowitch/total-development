name: Release 🚀
run-name: Release 🚀
on: 
    pull_request: 
        types:
            - opened
            - reopened
            - edited
            - synchronize

jobs:
    release:
        runs-on: ubuntu-latest
        steps:
        - run: docker build -t release ../../
        - run: docker run --rm -p 3000:3000 release
